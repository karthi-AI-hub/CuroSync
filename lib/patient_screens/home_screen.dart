import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Your Health Dashboard", style: TextStyle(fontSize: 20, color: Colors.white)),
    );
  }
}