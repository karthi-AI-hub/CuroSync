import 'package:flutter/material.dart';

class MediciensScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Mediciens Details", style: TextStyle(fontSize: 20)));
  }
}