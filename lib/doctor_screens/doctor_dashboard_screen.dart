import 'package:flutter/material.dart';

class DoctorDashboardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Doctor Dashboard", style: TextStyle(fontSize: 20)),
    );
  }
}