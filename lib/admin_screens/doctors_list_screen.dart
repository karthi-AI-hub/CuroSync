import 'package:flutter/material.dart';

class DoctorsListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Doctors List", style: TextStyle(fontSize: 20, color: Colors.white)),
    );
  }
}