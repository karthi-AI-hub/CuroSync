import 'package:flutter/material.dart';

class PatientsListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Patients List", style: TextStyle(fontSize: 20, color: Colors.white),),
    );
  }
}